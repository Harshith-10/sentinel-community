# --- Builder for Sentinel Application ---
# This Dockerfile builds the application.
# The resulting image should be tagged as sentinel-app-builder
# and can be used as a builder stage for other images.
FROM node:22-alpine
WORKDIR /usr/src/app
COPY package*.json ./
COPY tsconfig.json ./
RUN npm ci
COPY src/ ./src/
COPY config/ ./config/
RUN npm run build