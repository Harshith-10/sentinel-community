apiVersion: apps/v1
kind: Deployment
metadata:
  name: typescript-executor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: typescript-executor
  template:
    metadata:
      labels:
        app: typescript-executor
    spec:
      runtimeClassName: gvisor
      securityContext:
        seccompProfile:
          type: Localhost
          localhostProfile: seccomp-profile.json
      containers:
      - name: typescript-executor
        image: harshithd/sentinel-executor-typescript:latest
        imagePullPolicy: IfNotPresent
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        env:
        - name: REDIS_HOST
          value: "redis"
        - name: REDIS_PORT
          value: "6379"
        - name: LANGUAGE
          value: "typescript"
        - name: EXECUTOR_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        resources:
          requests:
            cpu: "500m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"