apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: javascript-executor-scaler
spec:
  scaleTargetRef:
    name: javascript-executor
  pollingInterval: 15
  cooldownPeriod:  30
  minReplicaCount: 1
  maxReplicaCount: 15
  triggers:
  - type: redis
    metadata:
      address: redis:6379
      listName: bull:javascript-executor:wait
      listLength: "5"