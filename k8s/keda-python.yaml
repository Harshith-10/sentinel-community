apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: python-executor-scaler
spec:
  scaleTargetRef:
    name: python-executor
  pollingInterval: 15
  cooldownPeriod:  30
  minReplicaCount: 1
  maxReplicaCount: 15
  triggers:
  - type: redis
    metadata:
      address: redis:6379
      listName: bull:python-executor:wait
      listLength: "5"