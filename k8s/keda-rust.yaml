apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: rust-executor-scaler
spec:
  scaleTargetRef:
    name: rust-executor
  pollingInterval: 15
  cooldownPeriod:  30
  minReplicaCount: 1
  maxReplicaCount: 10
  triggers:
  - type: redis
    metadata:
      address: redis:6379
      listName: bull:rust-executor-1:wait # Adjust if your queue naming is different
      listLength: "5"